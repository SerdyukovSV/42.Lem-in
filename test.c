#include "./includes/lemin.h"

int main(int ac, char **av)
{

    printf("len = %d\n", ft_wordcount(av[1], ' '));
    // ft_memcpy(arr2, arr, sizeof(int) * 5);
    // ft_bzero(arr, sizeof(int) * 5);
    // int i = -1;
    // while (++i < 5)
    //     printf("%d\t", arr2[i]);
    // printf("\n");
    return (0);
}